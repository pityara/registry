import { makeStyles } from '@material-ui/core';

export default makeStyles({
  container: {
    width: '100%',
    display: 'flex',
    flexDirection: 'column',

  },
  button: {
    alignSelf: 'flex-end',
    marginBottom: '2rem',
  }
})
