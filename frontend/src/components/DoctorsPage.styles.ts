import { makeStyles } from '@material-ui/core';

export default makeStyles({
  container: {
    width: '100%'
  },
  timeSelection: {
    display: 'flex',
    flexDirection: 'column',
  },
  field: {
    marginBottom: '1rem',
  }
});
