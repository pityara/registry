import { makeStyles } from '@material-ui/core';

export default makeStyles({
  auth: {
    display: 'flex',
    alignItems: 'center',
    margin: '0 1rem',
  },
  hello: {
    marginRight: '1rem'
  }
})
